--- /tmp/avrdude-5.8/Makefile.am	2009-03-06 15:09:11.000000000 -0500
+++ avrdude-5.8/Makefile.am	2011-05-14 07:56:52.912218185 -0400
@@ -158,6 +158,9 @@
 distclean-local:
 	rm -f avrdude.conf
 
+avrdude.conf: avrdude.conf.tmp
+	sed $(confsubst) < avrdude.conf.tmp > avrdude.conf
+
 # This will get run before the config file is installed.
 backup-avrdude-conf:
 	@echo "Backing up avrdude.conf in ${DESTDIR}${sysconfdir}"
--- /tmp/avrdude-5.8/configure.ac	2009-07-10 18:52:03.000000000 -0400
+++ avrdude-5.8/configure.ac	2011-05-14 08:04:50.648397674 -0400
@@ -239,7 +239,7 @@
 else
 	confsubst="-e /^@HAVE_PARPORT_BEGIN@/,/^@HAVE_PARPORT_END@/d"
 fi
-export confsubst
+AC_SUBST(confsubst)
 
 # See if we need to drop into the windows subdir.
 case $target in
@@ -269,9 +269,8 @@
 # avrdude.conf.tmp.  Finally, a sed command will be applied to filter
 # out unwanted parts (currently the parallel port programmer types)
 # based on previous configuration results, thereby producing the final
-# avrdude.conf file.
+# avrdude.conf file.  The sed command is in the Makefile.
 
-AC_CONFIG_FILES([avrdude.conf.tmp:avrdude.conf.in],
-		[sed $confsubst avrdude.conf.tmp > avrdude.conf])
+AC_CONFIG_FILES([avrdude.conf.tmp:avrdude.conf.in])
 
 AC_OUTPUT
--- /tmp/avrdude-5.8/configure	2009-07-10 18:52:11.000000000 -0400
+++ avrdude-5.8/configure	2011-05-14 10:05:37.142758521 -0400
@@ -2023,7 +2023,7 @@
   program_prefix=${target_alias}-
 
 
-am__api_version='1.10'
+am__api_version='1.11'
 
 # Find a good install program.  We prefer a C program (faster),
 # so one script is as good as another.  But avoid the broken or
